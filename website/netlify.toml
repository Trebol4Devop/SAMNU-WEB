[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[dev]
  publish = "dist"
  port = 8888
  target = "static"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
